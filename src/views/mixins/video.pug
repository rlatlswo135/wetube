mixin video(video)
    div
        h4
            a(href=`/videos/${video.id}`) #{video.title}
        p #{video.description}
        small #{video.createAt}
        hr

//- 들여쓰기 주의! Mixin과 div가 동일선상이면 안되지
//- mixin은 partials에서 변수가 들어간 버전이라생각하자
//- 그냥 js의 모듈함수라생각하자 => 당연히 import도 해야겠지?
//- include로 불러오는거.
//- react의 컴포넌트같은 느낌.
