doctype html
html(lang="ko")
    head
        title Wetube | #{pageTitle}
        link(rel="stylesheet" href="https://unpkg.com/mvp.css")
    body 
        header 
            h1 #{pageTitle}
            nav
                ul 
                    li
                        a(href="/") Home 

                    li
                        a(href="/search") Search Video
                        if isLogin
                            li
                                a(href="/videos/upload") Upload Video
                            li
                                a(href="/users/edit") Edit Profile
                            li
                                a(href="/users/logout") Logout 
                            p Heloo #{user.userName}
                        else 
                            li
                                a(href="/join") 회원가입
                            li 
                                a(href="/login") Login

            block header

        main 
            block content
            //- block은 실리콘틀 같은느낌이다 => 내가알아서 모양잡고 쓰면된다는거
            //- 결국 block가 있다는건. 다른 pug가 확장해서 그 블록을 커스텀할수 있다는얘기..
            include partials/footer.pug
            //- 모듈처럼 움직일수있네. scss랑 비슷한듯? => pug -> include(문서)